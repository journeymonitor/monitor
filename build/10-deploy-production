#!/bin/bash

PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:$PATH

rsync -ac --stats $2/ /opt/selenior/monitor/ || exit 1

cd /opt/selenior/monitor/

sudo -u selenior PHP_ENV=prod /usr/bin/php ./vendor/davedevelopment/phpmig/bin/phpmig migrate || exit 1
